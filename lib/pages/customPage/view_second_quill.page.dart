import 'package:flutter/material.dart';

class ViewSecondQuillPage extends StatelessWidget {
  const ViewSecondQuillPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Second Quill'),
      ),
      body: Column(
        children: [],
      ),
    );
  }
}
